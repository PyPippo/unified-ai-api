{
  "_comment": "Template file for API keys and secrets",
  "_description": "This file contains API keys referenced by config_name from providers.json. Never commit actual API keys to version control!",
  "_instructions": [
    "1. Copy this file and rename the copy to 'secret.json'",
    "2. Replace placeholder values with your actual API keys",
    "3. Make sure secret.json is excluded in .gitignore",
    "4. For production, consider using environment variables instead"
  ],
  "_security_warning": "🔒 NEVER commit actual API keys to version control! Use .gitignore to exclude secret.json",

  "gemma-3-4b-it:free": "sk-or-v1-YOUR_OPENROUTER_API_KEY_HERE",
  "llama-3-1-70b-instruct:fireworks-ai": "hf_YOUR_HUGGINGFACE_TOKEN_HERE",
  "Ling-1T:featherless-ai": "hf_YOUR_HUGGINGFACE_TOKEN_HERE",

  "_example_usage": {
    "description": "Keys are referenced by the 'config_name' field in providers.json",
    "example": "Each key in this file corresponds to a config_name from providers.json"
  }
}